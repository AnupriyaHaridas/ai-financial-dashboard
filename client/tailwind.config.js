/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,ts,jsx,tsx}",
      // If using other file types, add them to the list above
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }
  